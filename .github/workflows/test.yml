name: CI

on:
  schedule:
    - cron:  '55 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Post webhook test
      run: curl -X POST -H 'Content-Type:application/json' -d '{"value1":"1つ目"}' "https://maker.ifttt.com/trigger/${EVENT}/with/key/${TOKEN}"
      env:
        EVENT: ${{ secrets.WEBHOOK_EVENT }}
        TOKEN: ${{ secrets.WEBHOOK_TOKEN }}
